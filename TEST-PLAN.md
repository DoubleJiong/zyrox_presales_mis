# 售前支撑系统——交付前完整测试计划

> **文档版本：** v1.0  
> **适用范围：** 系统全量交付前回归验收  
> **覆盖模块：** 工作台、日程管理、客户管理、项目管理、任务中心、人员档案、解决方案、预警管理、消息中心、系统设置  
> **优先级说明：** P1 = 阻塞级（必须通过）、P2 = 高优先（强烈建议通过）、P3 = 普通（尽量通过）

---

## 一、测试计划概述

### 1.1 测试目的

通过系统化的功能验证、权限边界测试及集成场景测试，确认系统达到可交付标准，核心业务流程无阻塞性缺陷。

### 1.2 测试范围

| 类型 | 说明 |
|------|------|
| 功能测试 | 验证各模块全部 CRUD 及业务操作符合需求 |
| 权限测试 | 验证 RBAC + 数据权限在各角色下的隔离效果 |
| 集成测试 | 验证跨模块联动（消息通知、工作台汇总、任务调度等） |
| 边界/异常测试 | 必填项校验、非法参数、越权访问等 |
| UI/UX 验收 | 关键页面加载、列表分页、表单交互符合预期 |

### 1.3 不在范围内

- 性能压力测试
- 数据大屏专项测试（由独立测试计划覆盖）
- 移动端适配测试

---

## 二、测试环境与测试账户

### 2.1 环境要求

- 系统正常启动（Next.js dev 或 build 产物），访问 `http://localhost:3000`
- 数据库已完成 `drizzle-kit migrate`，所有迁移脚本已执行
- Redis/SSE 服务正常（用于实时消息推送）
- 已完成基础数据字典初始化（行业、区域、优先级等）

### 2.2 测试账户清单

| 账户标识 | 角色 | 用途 |
|----------|------|------|
| `test_super@example.com` | Super Admin（超级管理员） | 全权操作、系统设置 |
| `test_admin@example.com` | Admin（管理员） | 日常管理操作 |
| `test_mgr@example.com` | Dept Manager（部门经理） | 部门级操作 |
| `test_sales@example.com` | Sales（销售） | 常规业务操作 |
| `test_viewer@example.com` | Viewer（只读） | 只读访问验证 |
| `test_sales2@example.com` | Sales（销售，另一人） | 数据权限隔离验证 |

### 2.3 基础测试数据要求

在开始模块测试前，请确保已创建：

- 至少 3 个客户（含不同状态：active / potential / lost）
- 至少 2 个项目（含不同阶段）
- 至少 1 个已发布的解决方案
- 至少 2 条预警规则（1 条启用、1 条停用）
- 至少 5 条不同类型的消息

---

## 三、通用认证与鉴权测试

| # | 场景 | 操作步骤 | 预期结果 | 优先级 |
|---|------|----------|----------|--------|
| AUTH-01 | 未登录访问受保护页 | 直接访问 `/workbench` | 302 跳转至 `/login` | P1 |
| AUTH-02 | 登录成功跳转 | 使用正确账密登录 | 跳转至工作台首页 | P1 |
| AUTH-03 | 错误密码提示 | 输入错误密码登录 | 显示"用户名或密码错误"，不跳转 | P1 |
| AUTH-04 | Token 过期处理 | 手动删除/过期 cookie，访问 API | 返回 401，前端跳转登录页 | P1 |
| AUTH-05 | Viewer 角色不可访问写接口 | Viewer 账户发 `POST /api/customers` | 返回 403 | P1 |
| AUTH-06 | 跨用户数据访问（数据权限） | `test_sales2` 访问 `test_sales` 创建的客户（仅自己模式） | 返回 403 或记录不可见 | P1 |
| AUTH-07 | 登出后会话失效 | 登出后直接访问受保护路由 | 跳转登录页 | P2 |

---

## 四、各模块功能测试

---

### 4.1 工作台（Workbench）

**功能概述：** 聚合展示待处理事项、跟进中项目、未读消息、风险预警及本周任务的仪表盘。

#### 4.1.1 核心功能测试

| # | 场景 | 操作步骤 | 预期结果 | 优先级 |
|---|------|----------|----------|--------|
| WB-01 | 工作台页面正常加载 | 登录后访问 `/workbench` | 页面加载完成，各指标卡片正常渲染，无白屏 | P1 |
| WB-02 | 待处理事项数量正确 | 在任务中心新建一条待处理任务后，返回工作台 | "待处理事项"计数 +1 | P1 |
| WB-03 | 未读消息数量正确 | 在消息中心确认有 N 条未读后查看工作台 | "未读消息"数值与消息中心一致 | P1 |
| WB-04 | 风险预警展示 | 已存在触发的预警记录 | 预警卡片显示最新预警条目 | P1 |
| WB-05 | 本周任务列表 | 当前用户有本周内到期的任务 | 工作台列出对应任务，含任务名称和截止日期 | P2 |
| WB-06 | 最近动态 Feed | 系统有近期操作（新消息、预警触发等） | Feed 区域展示时间倒序的动态 | P2 |
| WB-07 | 快捷导航 | 点击工作台中的"查看项目"等快捷入口 | 正确跳转至对应模块页面 | P2 |
| WB-08 | 周报/月报入口 | 点击"周报"按钮 | 弹出报告生成界面或跳转至报告页 | P3 |
| WB-09 | 数据刷新 | 刷新页面 | 各指标数据更新至最新值 | P2 |
| WB-10 | SSE 实时推送 | 打开工作台，在另一浏览器窗口触发一条新消息 | 工作台未读消息数在 5 秒内自动更新（无需刷新） | P2 |

#### 4.1.2 权限测试

| # | 场景 | 预期结果 | 优先级 |
|---|------|----------|--------|
| WB-P01 | Viewer 账户访问工作台 | 正常显示只读汇总数据，无"新建"等操作按钮 | P2 |
| WB-P02 | 工作台汇总数据仅显示当前用户权限内的数据 | Sales 用户的"跟进中项目"仅含自己负责的项目 | P1 |

---

### 4.2 日程管理（Schedule Management）

**功能概述：** 支持个人日程的创建、编辑、删除，支持月/周/日/列表多视图切换，含参与者协同、重复规则与提醒。

#### 4.2.1 创建功能

| # | 场景 | 操作步骤 | 预期结果 | 优先级 |
|---|------|----------|----------|--------|
| SCH-01 | 创建最小日程 | 填写标题 + 开始时间，提交 | 日程创建成功，列表中可见 | P1 |
| SCH-02 | 创建含全部字段的日程 | 填写标题、描述、地点、参与者、重复规则、提醒，提交 | 日程创建成功，所有字段保存正确 | P1 |
| SCH-03 | 标题必填验证 | 清空标题直接提交 | 表单提示"标题不能为空" | P1 |
| SCH-04 | 结束时间早于开始时间 | 设置结束时间早于开始时间提交 | 表单提示时间范围错误 | P1 |
| SCH-05 | 添加参与者 | 创建时搜索并添加 `test_sales2` | 日程创建后，`test_sales2` 可在自己的日程列表中看到该日程 | P2 |

#### 4.2.2 查询与视图

| # | 场景 | 操作步骤 | 预期结果 | 优先级 |
|---|------|----------|----------|--------|
| SCH-06 | 月视图加载 | 切换至日历月视图 | 当月日程以事件块形式标注在对应日期 | P1 |
| SCH-07 | 周视图加载 | 切换至周视图 | 7 天网格展示当周日程 | P2 |
| SCH-08 | 列表视图分页 | 切换至列表视图，日程数 > 10 | 分页控件正常，翻页加载正确 | P2 |
| SCH-09 | 按状态筛选 | 使用状态筛选器选"已完成" | 仅显示 `scheduleStatus=completed` 的日程 | P2 |
| SCH-10 | 按日期范围筛选 | 设置开始/结束日期范围 | 仅返回该范围内的日程 | P2 |

#### 4.2.3 编辑与删除

| # | 场景 | 操作步骤 | 预期结果 | 优先级 |
|---|------|----------|----------|--------|
| SCH-11 | 编辑日程标题 | 打开日程详情，修改标题，保存 | 列表中显示新标题 | P1 |
| SCH-12 | 取消日程 | 将日程状态改为"已取消" | 状态更新，日程在列表中标注为"已取消" | P2 |
| SCH-13 | 删除日程 | 点击删除，确认弹窗 | 日程从列表中移除，API 返回 200 | P1 |
| SCH-14 | 删除他人日程（越权） | `test_sales` 尝试删除 `test_sales2` 的日程 | 返回 403 | P1 |

#### 4.2.4 工作台联动

| # | 场景 | 预期结果 | 优先级 |
|---|------|----------|--------|
| SCH-15 | 今日任务关联日程出现在工作台焦点队列 | 创建一条 `relatedType=task`、日期为今天的日程后，工作台的焦点队列中可见该日程 | P2 |

---

### 4.3 客户管理（Customer Management）

**功能概述：** 客户全生命周期管理，含客户档案、跟进记录、关联项目、批量导入/导出。

#### 4.3.1 创建与编辑

| # | 场景 | 操作步骤 | 预期结果 | 优先级 |
|---|------|----------|----------|--------|
| CUS-01 | 创建客户（必填信息） | 填写客户名称，提交 | 客户创建成功，跳转至详情或列表 | P1 |
| CUS-02 | 创建客户（全字段） | 填写名称、区域、联系人、电话、邮箱、地址、描述 | 所有字段正确保存 | P1 |
| CUS-03 | 客户名称必填验证 | 清空名称直接提交 | 提示"客户名称不能为空" | P1 |
| CUS-04 | 邮箱格式验证 | 填写格式错误的邮箱（如 "abc@"），提交 | 表单提示"邮箱格式不正确" | P2 |
| CUS-05 | 手机号格式验证 | 填写非 11 位手机号，提交 | 表单提示格式错误 | P2 |
| CUS-06 | 编辑客户信息 | 修改已有客户的联系人姓名，保存 | 保存后详情页显示新联系人 | P1 |
| CUS-07 | 修改客户状态 | 将客户状态从 active 改为 lost | 状态更新，列表中状态标签变更 | P1 |

#### 4.3.2 查询与筛选

| # | 场景 | 操作步骤 | 预期结果 | 优先级 |
|---|------|----------|----------|--------|
| CUS-08 | 按名称搜索 | 输入客户名称关键字 | 列表仅显示匹配结果 | P1 |
| CUS-09 | 按状态筛选 | 选择 potential | 仅显示 `status=potential` 的客户 | P2 |
| CUS-10 | 按区域筛选 | 选择特定区域 | 仅显示该区域的客户 | P2 |
| CUS-11 | 分页功能 | 若客户总数 > 每页条数，使用分页控件 | 翻页正确，数据按 `updatedAt` 排序 | P2 |
| CUS-12 | 按合同金额排序 | 点击"合同金额"列头排序 | 列表按 `totalAmount` 降序/升序切换 | P3 |

#### 4.3.3 客户详情

| # | 场景 | 操作步骤 | 预期结果 | 优先级 |
|---|------|----------|----------|--------|
| CUS-13 | 查看关联项目 | 在客户详情切换至"项目"标签 | 显示该客户下所有项目，含项目名称、阶段、金额 | P1 |
| CUS-14 | 查看跟进记录 | 切换至"跟进记录"标签 | 显示时间倒序的跟进记录列表 | P2 |
| CUS-15 | 添加跟进记录 | 点击"添加跟进"，填写内容，提交 | 跟进记录出现在列表顶部 | P2 |

#### 4.3.4 批量操作

| # | 场景 | 操作步骤 | 预期结果 | 优先级 |
|---|------|----------|----------|--------|
| CUS-16 | 批量导入 | 上传合规的 Excel 文件 | 客户列表新增导入的记录，无重复 | P2 |
| CUS-17 | 导入模板格式错误 | 上传字段缺失的 Excel | 提示具体错误行与原因，不导入任何数据 | P2 |

#### 4.3.5 权限测试

| # | 场景 | 预期结果 | 优先级 |
|---|------|----------|--------|
| CUS-P01 | Viewer 不可创建客户 | 无"创建客户"按钮，直接调 API 返回 403 | P1 |
| CUS-P02 | Admin 不可删除客户 | Admin 角色无删除按钮（权限矩阵中无 `customer:delete`） | P1 |
| CUS-P03 | Sales 仅可见自己数据（数据权限=仅自己时） | `test_sales2` 看不到 `test_sales` 创建的客户 | P1 |
| CUS-P04 | 软删除不影响关联数据 | 删除客户后，其关联项目不被级联删除 | P2 |

---

### 4.4 项目管理（Project Management）

**功能概述：** 覆盖从商机到归档的 11 个项目阶段，含招投标、结算、实施、团队管理及合同管理。

#### 4.4.1 创建与基础信息

| # | 场景 | 操作步骤 | 预期结果 | 优先级 |
|---|------|----------|----------|--------|
| PRJ-01 | 创建项目（必填） | 填写项目名称 + 关联客户，提交 | 项目创建成功，初始阶段为 opportunity | P1 |
| PRJ-02 | 项目编码自动生成 | 新建项目提交后查看详情 | `projectCode` 已自动生成，格式符合规范 | P1 |
| PRJ-03 | 项目类型多选 | 选择多个项目类型，保存 | 详情页显示所有已选类型，数据正确存储 | P1 |
| PRJ-04 | 预算信息录入 | 填写人员、材料、差旅、其他类目预算，保存 | 费用预算各分项保存正确，合计自动计算 | P2 |
| PRJ-05 | 必填项验证 | 未选客户直接提交 | 提示"请选择客户" | P1 |

#### 4.4.2 阶段流转

| # | 场景 | 操作步骤 | 预期结果 | 优先级 |
|---|------|----------|----------|--------|
| PRJ-06 | 阶段正向推进 | 调用 `/api/projects/[id]/available-transitions`，按返回清单推进阶段 | 阶段变更成功，历史记录更新 | P1 |
| PRJ-07 | 非法阶段跳跃（越权） | 直接将 opportunity 阶段置为 settlement | API 返回 400/422，提示非法状态转换 | P1 |
| PRJ-08 | 招投标结果录入 | 进入 bidding 阶段，录入投标结果（中标/未中标） | `bidResult` 更新，若未中标则 `loseReason` 字段可选填 | P2 |
| PRJ-09 | 看板视图按阶段分组 | 切换至看板视图 | 11 个阶段列正确展示，项目卡片出现在对应阶段列 | P2 |

#### 4.4.3 多视图

| # | 场景 | 预期结果 | 优先级 |
|---|------|----------|--------|
| PRJ-10 | 表格视图列排序 | 可按"更新时间"、"预计金额"等列头排序 | P2 |
| PRJ-11 | 网格视图卡片渲染 | 每个项目卡片显示名称、阶段、客户、进度 | P2 |

#### 4.4.4 团队与子模块

| # | 场景 | 操作步骤 | 预期结果 | 优先级 |
|---|------|----------|----------|--------|
| PRJ-12 | 添加项目成员 | 在项目详情"成员"标签，搜索并添加用户 | 成员出现在成员列表，指定角色正确 | P1 |
| PRJ-13 | 查看关联解决方案 | 切换至"解决方案"标签 | 显示与该项目关联的解决方案列表 | P2 |
| PRJ-14 | 查看项目任务 | 切换至"任务"标签 | 显示该项目下所有任务 | P2 |
| PRJ-15 | 合同信息管理 | 切换至"合同"标签，录入合同签订日期与金额 | 合同信息保存，`contractAmount` 更新 | P2 |

#### 4.4.5 批量操作

| # | 场景 | 预期结果 | 优先级 |
|---|------|----------|--------|
| PRJ-16 | 批量删除（Super Admin） | 选中多个项目，批量删除 | 成功删除，列表刷新 | P2 |
| PRJ-17 | 批量导入项目 | 上传合规 Excel 文件 | 项目导入成功，无重复 | P2 |

#### 4.4.6 权限测试

| # | 场景 | 预期结果 | 优先级 |
|---|------|----------|--------|
| PRJ-P01 | Admin 无法删除项目 | 无删除按钮/调 API 返回 403 | P1 |
| PRJ-P02 | Sales 无法更改他人项目 | `test_sales2` 无法 PUT `test_sales` 创建的项目（数据权限=仅自己） | P1 |
| PRJ-P03 | Viewer 只读 | Viewer 无法看到创建/编辑/删除按钮 | P1 |

---

### 4.5 任务中心（Task Center）

**功能概述：** 项目任务与个人任务的统一管理，含优先级、状态跟踪、负责人指派及批量操作。

#### 4.5.1 任务 CRUD

| # | 场景 | 操作步骤 | 预期结果 | 优先级 |
|---|------|----------|----------|--------|
| TASK-01 | 创建任务（必填） | 填写任务名称 + 关联项目（如有），提交 | 任务创建成功，出现在列表 | P1 |
| TASK-02 | 字段完整性 | 填写任务名称、描述、负责人、优先级、开始/截止日期，提交 | 所有字段保存正确 | P1 |
| TASK-03 | 任务名称必填 | 清空名称提交 | 提示"任务名称不能为空" | P1 |
| TASK-04 | 截止日期早于开始日期 | 设置截止日期早于开始日期 | 表单提示日期范围错误 | P2 |
| TASK-05 | 更新任务状态 | 将任务状态改为 `in_progress` | 状态更新，列表标签变更 | P1 |
| TASK-06 | 完成任务并写入操作日志 | 将任务状态改为 `completed` | 状态更新；系统操作日志中出现"任务完成"条目 | P1 |
| TASK-07 | 删除任务 | 确认删除弹窗 | 任务从列表中移除 | P1 |

#### 4.5.2 进度与筛选

| # | 场景 | 预期结果 | 优先级 |
|---|------|----------|--------|
| TASK-08 | 按负责人筛选 | 仅显示指定负责人的任务 | P2 |
| TASK-09 | 按状态筛选（pending/in_progress/completed） | 正确过滤 | P1 |
| TASK-10 | 按优先级筛选（high/medium/low） | 正确过滤 | P2 |
| TASK-11 | 按项目筛选 | 仅显示指定项目的任务 | P2 |

#### 4.5.3 我的任务页

| # | 场景 | 操作步骤 | 预期结果 | 优先级 |
|---|------|----------|----------|--------|
| TASK-12 | 我的任务——全部 | 访问 `/my-tasks?type=all` | 显示当前用户的全部任务（预警任务 + 指派任务） | P1 |
| TASK-13 | 我的任务——预警任务 | 访问 `/my-tasks?type=alert` | 仅显示预警触发的任务 | P2 |
| TASK-14 | 我的任务——已指派任务 | 访问 `/my-tasks?type=assigned` | 仅显示 `assigneeId=当前用户` 的任务 | P2 |

#### 4.5.4 批量操作

| # | 场景 | 预期结果 | 优先级 |
|---|------|----------|--------|
| TASK-15 | 批量分配负责人 | 选中多条任务，批量更改负责人 | 所有选中任务的负责人更新 | P2 |

#### 4.5.5 工作台联动

| # | 场景 | 预期结果 | 优先级 |
|---|------|----------|--------|
| TASK-16 | 新任务出现在工作台的本周任务 | 创建一条截止日期在本周内、负责人为当前用户的任务 → 工作台"本周我的任务"计数 +1 | P2 |

---

### 4.6 人员档案（Staff Profiles）

**功能概述：** 系统内部人员的档案查阅，包含职务、部门、状态、关联项目/任务/客户的全关系视图。写操作统一由系统设置完成。

#### 4.6.1 查询功能

| # | 场景 | 操作步骤 | 预期结果 | 优先级 |
|---|------|----------|----------|--------|
| STF-01 | 员工列表加载 | 访问 `/staff` | 列表正常显示所有活跃员工 | P1 |
| STF-02 | 按姓名搜索 | 输入员工姓名关键字 | 仅显示匹配员工 | P1 |
| STF-03 | 按部门筛选 | 选择具体部门 | 仅显示该部门员工 | P2 |
| STF-04 | 按角色筛选 | 选择 Sales 角色 | 仅显示 Sales 角色员工 | P2 |
| STF-05 | 员工详情页 | 点击员工进入详情 | 显示姓名、职务、部门、入职日期、联系方式 | P1 |

#### 4.6.2 档案关系视图

| # | 场景 | 操作步骤 | 预期结果 | 优先级 |
|---|------|----------|----------|--------|
| STF-06 | 查看员工关联项目 | 在员工详情切换至"项目"标签或关系视图 | 显示该员工参与的项目列表 | P2 |
| STF-07 | 查看员工关联任务 | 切换至"任务"视图 | 显示指派给该员工的任务 | P2 |
| STF-08 | 查看员工关联客户 | 切换至"客户"视图 | 显示该员工负责的客户 | P2 |
| STF-09 | 员工近期活动 | 查看活动 Feed | 显示最近登录时间、最近操作记录（来自操作日志） | P3 |

#### 4.6.3 写操作重定向

| # | 场景 | 预期结果 | 优先级 |
|---|------|----------|--------|
| STF-10 | 人员档案无直接创建入口 | 页面无"创建员工"按钮，仅有"前往系统设置"入口 | P1 |
| STF-11 | 直接调写接口被拦截 | `POST /api/staff` 返回 405 或 403，提示使用设置页 | P1 |

---

### 4.7 解决方案（Solutions）

**功能概述：** 企业知识库管理，涵盖方案创建/发布、模板体系、版本控制、团队协作、方案评审及排行榜。

#### 4.7.1 创建与发布

| # | 场景 | 操作步骤 | 预期结果 | 优先级 |
|---|------|----------|----------|--------|
| SOL-01 | 创建草稿方案 | 填写名称 + 类型，保存为草稿 | 方案以 `status=draft` 创建 | P1 |
| SOL-02 | 发布方案 | 将草稿方案状态改为 published | `status` 更新为 published，`publishDate` 自动填入 | P1 |
| SOL-03 | 归档方案 | 将发布方案状态改为 archived | 方案不再出现在默认列表，可通过"归档"筛选显示 | P2 |
| SOL-04 | 方案名称必填 | 清空名称提交 | 表单提示"方案名称不能为空" | P1 |
| SOL-05 | 设为模板 | 点击"设为模板" | `isTemplate=true`，方案在模板列表可见 | P2 |
| SOL-06 | 从模板创建 | 点击模板方案的"从此模板新建" | 新方案预填模板内容，`status=draft` | P2 |

#### 4.7.2 查询与筛选

| # | 场景 | 预期结果 | 优先级 |
|---|------|----------|--------|
| SOL-07 | 按状态筛选（draft/published） | 正确过滤 | P1 |
| SOL-08 | 按行业筛选 | 仅显示该行业的方案 | P2 |
| SOL-09 | 快速标签——"我创建的" | 仅显示当前用户的方案 | P2 |
| SOL-10 | 快速标签——"模板" | 仅显示 `isTemplate=true` 的方案 | P2 |
| SOL-11 | 卡片/表格视图切换 | 视图切换流畅，数据不变 | P2 |

#### 4.7.3 方案详情

| # | 场景 | 操作步骤 | 预期结果 | 优先级 |
|---|------|----------|----------|--------|
| SOL-12 | 子板块（Sub-scheme）管理 | 创建新板块，录入内容，保存 | 板块出现在详情，顺序可调整 | P2 |
| SOL-13 | 版本历史 | 修改方案后查看版本历史标签 | 显示版本记录，含修改人和时间戳 | P2 |
| SOL-14 | 关联项目 | 在方案详情绑定项目 | 项目出现在"关联项目"列表，项目详情也可见该方案 | P2 |
| SOL-15 | 下载统计 | 下载方案附件后查看统计数据 | `downloadCount` +1 | P3 |
| SOL-16 | 浏览量统计 | 访问方案详情页 | `viewCount` 自动 +1 | P3 |

#### 4.7.4 团队协作

| # | 场景 | 操作步骤 | 预期结果 | 优先级 |
|---|------|----------|----------|--------|
| SOL-17 | 添加团队成员 | Owner 在成员标签添加 contributor 角色成员 | 成员可查看/编辑方案 | P2 |
| SOL-18 | 权限隔离 | 未添加的用户访问私有方案（`isPublic=false`） | 返回 403 | P1 |
| SOL-19 | 设置方案为公开 | 将 `isPublic` 置为 true | 所有登录用户可浏览该方案 | P2 |

#### 4.7.5 评审流程

| # | 场景 | 操作步骤 | 预期结果 | 优先级 |
|---|------|----------|----------|--------|
| SOL-20 | 提交评审 | 在方案评审标签提交评审请求 | 评审记录创建，通知发送给评审人 | P1 |
| SOL-21 | 评审人提交意见 | 评审人打开邀请，提交通过/拒绝评审意见 | 评审状态更新，原作者收到通知 | P1 |

#### 4.7.6 排行榜

| # | 场景 | 预期结果 | 优先级 |
|---|------|----------|--------|
| SOL-22 | 排行榜页面加载 | `/solutions/leaderboard` 正常加载，按浏览量/下载量排序 | P3 |

---

### 4.8 预警管理（Alert Management）

**功能概述：** 基于规则引擎的风险预警系统，涵盖规则配置、预警触发、处置记录及通知追踪。

#### 4.8.1 预警规则管理

| # | 场景 | 操作步骤 | 预期结果 | 优先级 |
|---|------|----------|----------|--------|
| ALT-01 | 创建预警规则 | 填写规则名称、目标类型、触发条件、严重级别，提交 | 规则创建成功，出现在规则列表 | P1 |
| ALT-02 | 规则名称必填 | 清空名称提交 | 表单提示不能为空 | P1 |
| ALT-03 | 启用/停用规则 | 切换规则启用状态 | `enabled` 字段更新，列表中状态标签变更 | P1 |
| ALT-04 | 编辑规则条件 | 修改已有规则的触发频率，保存 | 修改后的 `frequency` 正确持久化 | P2 |
| ALT-05 | 删除规则 | 删除一条预警规则 | 规则从列表移除，已触发的历史记录不受影响 | P2 |

#### 4.8.2 预警历史与处置

| # | 场景 | 操作步骤 | 预期结果 | 优先级 |
|---|------|----------|----------|--------|
| ALT-06 | 预警历史列表加载 | 访问 `/alerts/histories` | 显示所有预警记录，含触发时间、严重级别、处置状态 | P1 |
| ALT-07 | 按严重级别筛选 | 选择 critical | 仅显示严重级别为 critical 的记录 | P2 |
| ALT-08 | 按状态筛选（pending/resolved/dismissed） | 正确过滤 | P2 |
| ALT-09 | 处置预警——标记已解决 | 打开预警详情，将状态改为 resolved | 状态更新，处置时间记录 | P1 |
| ALT-10 | 处置预警——标记已忽略 | 将状态改为 dismissed | 状态更新，从"待处理"队列中移除 | P2 |
| ALT-11 | 查看预警详情 | 点击预警记录查看详情 | 详情页显示触发数据（alertData）和关联目标 | P2 |

#### 4.8.3 通知联动

| # | 场景 | 预期结果 | 优先级 |
|---|------|----------|--------|
| ALT-12 | 预警触发后接收人收到消息 | 预警规则触发时，`alertNotifications` 中的接收人在消息中心收到预警消息 | P1 |
| ALT-13 | 预警消息出现在工作台风险预警区域 | 新触发的 critical 预警出现在工作台摘要 | P2 |

#### 4.8.4 预警仪表盘

| # | 场景 | 预期结果 | 优先级 |
|---|------|----------|--------|
| ALT-14 | 仪表盘统计数据准确 | 预警规则总数、启用规则数、总预警数、待处理数与实际数据一致 | P2 |

---

### 4.9 消息中心（Message Center）

**功能概述：** 统一消息收发与管理平台，支持 8 种消息类型，含已读/未读管理、批量操作及 SSE 实时推送。

#### 4.9.1 消息列表

| # | 场景 | 操作步骤 | 预期结果 | 优先级 |
|---|------|----------|----------|--------|
| MSG-01 | 消息列表加载 | 访问 `/messages` | 列表按 `createdAt` 倒序展示消息，含类型图标 | P1 |
| MSG-02 | 按类型过滤——系统 | 切换至系统消息标签 | 仅显示 `type=system` 的消息 | P1 |
| MSG-03 | 按类型过滤——任务 | 切换至任务消息标签 | 仅显示 `type=task` 的消息 | P1 |
| MSG-04 | 按类型过滤——提及 | 切换至提及标签 | 仅显示 `type=mention` 的消息 | P1 |
| MSG-05 | 按类型过滤——批准 | 切换至批准标签 | 仅显示 `type=approval` 的消息 | P2 |
| MSG-06 | 仅显示未读 | 开启"仅未读"过滤 | 仅显示 `isRead=false` 的消息 | P2 |
| MSG-07 | 分页翻页 | 消息数 > 每页条数，翻至第 2 页 | 加载下一批消息，无数据丢失 | P2 |

#### 4.9.2 已读管理

| # | 场景 | 操作步骤 | 预期结果 | 优先级 |
|---|------|----------|----------|--------|
| MSG-08 | 标记单条消息为已读 | 点击消息的"标记已读"，或 POST `/messages/[id]/read` | 消息变为已读，未读计数 -1 | P1 |
| MSG-09 | 标记全部已读 | 点击"全部标记为已读" | 所有消息变为 `isRead=true`，未读计数归零 | P1 |
| MSG-10 | 标记已读写入操作日志 | 标记读后查看操作日志 | 操作日志中存在对应条目 | P2 |

#### 4.9.3 消息删除

| # | 场景 | 操作步骤 | 预期结果 | 优先级 |
|---|------|----------|----------|--------|
| MSG-11 | 删除单条消息 | 点击消息的"删除"按钮，确认 | 消息从列表移除 | P1 |
| MSG-12 | 删除他人消息（越权） | `test_sales2` 尝试删除 `test_sales` 的消息 | 返回 403 | P1 |

#### 4.9.4 通知铃铛（Notification Bell）

| # | 场景 | 操作步骤 | 预期结果 | 优先级 |
|---|------|----------|----------|--------|
| MSG-13 | 铃铛未读计数正确 | 消息中心有 N 条未读，查看顶部铃铛 | 铃铛角标显示 N（超 99 则显示 99+） | P1 |
| MSG-14 | 铃铛下拉列表 | 点击铃铛 | 下拉展示最新的未读消息预览，含标题和时间 | P2 |
| MSG-15 | 铃铛标记已读联动 | 在铃铛下拉中标记一条消息已读 | 铃铛计数 -1，消息中心对应消息变为已读 | P1 |
| MSG-16 | 铃铛批量已读 | 在铃铛下拉中点击"全部已读" | 铃铛计数归零，消息中心所有消息变为已读 | P2 |

#### 4.9.5 实时推送（SSE）

| # | 场景 | 操作步骤 | 预期结果 | 优先级 |
|---|------|----------|----------|--------|
| MSG-17 | SSE 连接建立 | 登录后查看 `/api/events` 是否正常返回 SSE stream | 连接建立，保持长连接，返回 `text/event-stream` | P1 |
| MSG-18 | 新消息实时推送 | 打开消息中心，在另一会话中向当前用户发一条消息 | 铃铛计数在 5 秒内自动更新，无需刷新 | P1 |

#### 4.9.6 管理员视角

| # | 场景 | 操作步骤 | 预期结果 | 优先级 |
|---|------|----------|----------|--------|
| MSG-19 | 超级管理员查看全部消息 | 超级管理员访问 `GET /api/messages?viewAs=admin` | 返回全体用户的消息（不限 receiverId） | P2 |
| MSG-20 | 非超级管理员无法使用 viewAs=admin | 普通用户访问 `GET /api/messages?viewAs=admin` | 返回 403 或仅返回自己的消息 | P1 |

---

### 4.10 系统设置（System Settings）

**功能概述：** 系统基础配置平台，涵盖数据字典、角色权限、用户管理、公司管理、项目类型、操作日志及数据备份。

#### 4.10.1 数据字典

| # | 场景 | 操作步骤 | 预期结果 | 优先级 |
|---|------|----------|----------|--------|
| SET-01 | 字典类型列表加载 | 访问数据字典页 | 显示所有字典类型（行业、区域、优先级等） | P1 |
| SET-02 | 新增字典项目 | 选择字典类型，添加新选项，保存 | 新选项出现在对应业务模块的下拉菜单中 | P1 |
| SET-03 | 编辑字典项目名称 | 修改已有字典项目的显示名称 | 名称更新，对应模块下拉选项同步变更 | P2 |
| SET-04 | 删除字典项目 | 删除一个未被引用的字典项目 | 项目从字典列表中移除 | P2 |
| SET-05 | 调整字典项目排序 | 拖拽调整 `sortOrder` | 保存后下拉菜单中按新顺序展示 | P3 |

#### 4.10.2 角色权限配置

| # | 场景 | 操作步骤 | 预期结果 | 优先级 |
|---|------|----------|----------|--------|
| SET-06 | 角色列表加载 | 访问 `/settings/roles` | 显示 5 个预设角色及其权限摘要 | P1 |
| SET-07 | 新建自定义角色 | 填写角色名称，勾选部分权限，保存 | 自定义角色创建，可分配给用户 | P2 |
| SET-08 | 修改角色权限 | 为 Sales 角色取消 `solution:delete` 权限 | 拥有 Sales 角色的用户立即失去删除方案的行为 | P1 |
| SET-09 | 删除自定义角色 | 删除一个无用户绑定的自定义角色 | 角色删除成功 | P2 |
| SET-10 | 级联影响验证 | 修改角色权限后，使用该角色账号操作 | 权限变更立即生效（需重新登录或刷新 token） | P1 |

#### 4.10.3 数据权限配置

| # | 场景 | 操作步骤 | 预期结果 | 优先级 |
|---|------|----------|----------|--------|
| SET-11 | 配置角色数据权限为"仅自己" | 将 Sales 角色数据权限改为 `仅自己` | `test_sales` 不可见 `test_sales2` 的客户/项目数据 | P1 |
| SET-12 | 配置数据权限为"全部" | 将 Sales 角色数据权限改为 `全部` | `test_sales` 可见所有客户/项目数据 | P2 |

#### 4.10.4 用户管理

| # | 场景 | 操作步骤 | 预期结果 | 优先级 |
|---|------|----------|----------|--------|
| SET-13 | 用户列表加载 | 访问 `/settings/users` | 显示所有用户，含角色、部门、状态 | P1 |
| SET-14 | 创建新用户 | 填写用户名、邮箱、角色，提交 | 用户创建成功，可使用设定密码登录 | P1 |
| SET-15 | 邮箱唯一性验证 | 创建用户时使用已存在的邮箱 | 提示"邮箱已存在" | P1 |
| SET-16 | 修改用户角色 | 将 `test_viewer` 角色改为 Sales | 用户角色更新，重登后获得 Sales 权限 | P1 |
| SET-17 | 禁用用户 | 将用户状态改为 inactive | 该用户无法登录，尝试登录时提示"账号已禁用" | P1 |
| SET-18 | 删除用户 | 删除一个非当前登录用户 | 从用户列表移除，相关关联记录（操作日志）保留 | P2 |

#### 4.10.5 公司管理

| # | 场景 | 预期结果 | 优先级 |
|---|------|----------|--------|
| SET-19 | 新增子公司 | 创建新的承接公司，信息保存正确 | P2 |
| SET-20 | 编辑公司信息 | 修改公司名称，保存后在相关模块的选择器中同步 | P2 |

#### 4.10.6 操作日志

| # | 场景 | 操作步骤 | 预期结果 | 优先级 |
|---|------|----------|----------|--------|
| SET-21 | 操作日志列表加载 | 访问 `/settings/system-logs` | 显示最近操作日志，含操作人、模块、操作类型、时间 | P1 |
| SET-22 | 日志无法被手动删除 | 尝试通过任意方式删除日志条目 | 无删除入口；通过 API 直接删除返回 405/403 | P2 |
| SET-23 | 关键操作有日志记录 | 完成客户编辑、任务完成、报价审批等操作后查看日志 | 对应的操作日志条目存在 | P1 |

#### 4.10.7 数据备份（基础验证）

| # | 场景 | 预期结果 | 优先级 |
|---|------|----------|--------|
| SET-24 | 备份功能入口可访问 | `/settings/data-backup` 页面正常加载，无 500 错误 | P2 |

---

## 五、集成场景测试

以下测试验证多个模块的联动，是交付前最重要的端到端验证。

| # | 场景名称 | 涉及模块 | 操作步骤 | 预期结果 | 优先级 |
|---|----------|----------|----------|----------|--------|
| INT-01 | 项目创建→任务指派→消息通知全链路 | 项目、任务、消息 | 创建项目，新建任务并指派给 `test_sales2`，验证消息中心 | `test_sales2` 收到任务分配消息，类型为 `task` | P1 |
| INT-02 | 预警触发→消息推送→工作台更新 | 预警、消息、工作台 | 触发一条 critical 预警 | 预警历史中有新记录；接收人消息中心有 `alert` 类型消息；工作台风险预警区域出现该条目 | P1 |
| INT-03 | 解决方案评审→通知链路 | 解决方案、消息 | 提交方案评审 | 评审人收到通知；评审提交意见后原作者收到通知 | P1 |
| INT-04 | 报价提交→审批→通知链路 | 项目（报价）、消息 | 提交报价单，审批通过/拒绝 | 申请人收到审批结果消息；操作日志中有审批记录 | P1 |
| INT-05 | 日程任务关联→工作台焦点队列 | 日程、任务、工作台 | 创建今日的任务关联日程（relatedType=task，负责人=当前用户）| 工作台焦点队列中出现该日程，不重复 | P2 |
| INT-06 | 客户归属方案 | 客户、项目、解决方案 | 将解决方案关联至某项目后，从客户详情的项目标签进入该项目的解决方案标签 | 方案可见，跳转路径正确 | P2 |
| INT-07 | 用户角色变更→权限即时生效 | 系统设置、各业务模块 | 在系统设置中修改某用户角色，该用户在新 session 中访问被限制的功能 | 权限如预期生效，原本可见的入口不再可见 | P1 |
| INT-08 | 仲裁（Arbitration）创建→通知 | 仲裁、消息 | 提交仲裁申请 | 相关责任人收到仲裁通知消息 | P2 |

---

## 六、权限边界汇总测试

以下为按角色汇总的关键权限验证，测试时使用对应账户。

| 角色 | 测试验证点 | 预期结果 | 优先级 |
|------|-----------|----------|--------|
| **Super Admin** | 可访问系统设置的全部子页面 | 全部可访问 | P1 |
| **Super Admin** | 可查看全部用户的消息（viewAs=admin） | 正常返回全量消息 | P2 |
| **Admin** | 不含 `customer:delete`，无法删除客户 | 删除按钮不可见或 API 返回 403 | P1 |
| **Admin** | 不含 `project:delete`，无法删除项目 | 同上 | P1 |
| **Dept Manager** | 与 Admin 同级，不含 `settings:update` | 系统设置页为只读或不可访问 | P1 |
| **Sales** | 可进行客户/项目 CRUD | 所有写操作正常 | P1 |
| **Sales** | 无 `settings:update` | 系统设置配置项无法修改 | P1 |
| **Sales** | 无 `log:view` | `/settings/system-logs` 返回 403 或无法访问 | P2 |
| **Viewer** | 全系统只读 | 所有新建/编辑/删除按钮隐藏或 API 返回 403 | P1 |
| **Viewer** | 无 `settings:view` | 系统设置不可进入 | P1 |

---

## 七、UI/UX 验收标准

| # | 项目 | 验收标准 | 优先级 |
|---|------|----------|--------|
| UX-01 | 页面首屏加载时间 | 主要页面（工作台、客户列表、项目列表）在网络正常时首屏 < 3 秒 | P2 |
| UX-02 | 表单提交反馈 | 所有提交按钮在请求期间显示 loading 状态，防止重复提交 | P2 |
| UX-03 | 错误提示友好 | API 500 错误时，前端不展示裸 JSON，而是显示"操作失败，请重试" | P1 |
| UX-04 | 删除确认弹窗 | 所有删除操作必须通过二次确认弹窗（不可直接删除） | P1 |
| UX-05 | 空状态展示 | 列表无数据时显示友好的空状态提示，而非白屏或无提示 | P2 |
| UX-06 | 分页一致性 | 所有列表页的分页控件样式与行为一致 | P2 |
| UX-07 | 响应式适配（最低宽度） | 页面在 1280px 宽度下无横向溢出，关键操作可访问 | P3 |
| UX-08 | 必填项标识 | 所有表单中的必填项标有红色星号（`*`） | P2 |
| UX-09 | 导航高亮 | 当前所在模块的侧边栏导航项高亮 | P2 |
| UX-10 | 面包屑/页面标题 | 每个页面有清晰的标题或面包屑，用户知晓当前位置 | P2 |

---

## 八、测试执行记录模板

| 测试用例 ID | 执行人 | 执行日期 | 实际结果 | 状态（通过/失败/待复测） | 备注 |
|------------|--------|----------|----------|--------------------------|------|
| AUTH-01 | | | | | |
| WB-01 | | | | | |
| … | | | | | |

---

## 九、交付判定标准

| 级别 | 条件 | 决策 |
|------|------|------|
| **可交付** | 全部 P1 测试用例通过；P2 通过率 ≥ 90%；无未关闭的阻塞性 Bug | 正常交付 |
| **有条件交付** | 全部 P1 通过；P2 通过率 ≥ 75%；已知 Bug 已记录并安排修复计划 | 附条件交付，限期修复 |
| **不可交付** | 任意 P1 测试用例未通过；或存在未修复的安全漏洞（越权、注入等） | 停止交付，需先修复 |

---

*本测试计划生成自代码库自动分析，如业务逻辑有更新，请同步修订对应章节。*
