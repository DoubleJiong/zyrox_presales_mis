# 测试数据隔离说明

第一阶段测试基线只允许使用本地可控环境，不允许依赖生产数据库、共享测试账号或人工维护的远程状态。

约束如下：

1. `tests/unit/` 只能验证纯函数、配置读取和 policy 逻辑，不访问真实数据库。
2. `tests/api/` 优先使用 mock、受控测试请求或本地隔离环境，不依赖生产库和真实共享账号。
3. `tests/e2e/` 的 smoke 场景只验证本地可稳定启动的页面和受保护路由基础行为，不把生产数据当作前置条件。
4. 新增测试时，如果必须访问数据库，必须先说明数据来源、清理方式和失败后的回滚办法。