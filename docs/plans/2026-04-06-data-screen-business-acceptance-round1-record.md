# 数据大屏首轮业务陪跑记录

日期：2026-04-06

关联脚本：`docs/plans/2026-04-06-data-screen-business-acceptance-scripts.md`

关联样本：`docs/plans/2026-03-30-phase-5-business-acceptance-sample-list.md`

## 1. 记录边界

本记录不是业务最终签字单。

本轮目标是把 Task 8 从“只有脚本模板”推进到“已有一轮正式环境基线记录”，为后续真实业务账号陪跑减少重复准备成本。

本记录依据以下正式基线生成：

1. BUILD_ID：`40cwCdEZCO3XMKRR7_0HL`
2. 运行口径：`next start -p 5004`
3. 统计区间：`2026-03-07` 至 `2026-04-06`
4. 数据来源：`/api/data-screen/overview` 与 `/api/data-screen/presales-focus-summary`
5. 技术门禁：`verify:acceptance:5004 = 40 passed (1.6m)`

## 2. 管理层首轮记录

### 2.1 首屏读数

| 指标 | 当前值 | 对应来源 |
| --- | ---: | --- |
| 客户总数 | 372 | `overview.data.totalCustomers` |
| 项目总数 | 504 | `overview.data.totalProjects` |
| 中标项目 | 69 | `overview.data.wonProjects` |
| 覆盖率 | 290% | `forecastSummary.coverageRate` |
| 目标基线 | ¥6266万 | `forecastSummary.targetAmount` |
| 预测完成 | ¥18190万 | `forecastSummary.forecastAmount` |
| 预测缺口 | ¥0.0万 | `forecastSummary.gapAmount` |
| 风险总量 | 0 | `riskSummary.total` |
| 高风险 | 0 | `riskSummary.high` |

### 2.2 区域贡献观察

当前 `topRegions` 返回前 3 位如下：

| 排名 | 区域 | 热点值 | 金额 |
| --- | --- | ---: | ---: |
| 1 | 杭州 | 59 | ¥283.6万 |
| 2 | 山东 | 45 | ¥556.0万 |
| 3 | 温台 | 27 | ¥3200.0万 |

### 2.3 首轮判定

1. 管理层首屏问题“目标是否覆盖、风险是否突出”当前可以直接回答。
2. drill-through 与自动化验证已证明入口可达 canonical 项目页。
3. 本轮记录暴露出“区域贡献 TOP3”实际排序更像“热点 TOP3”；该问题已在同日后续修正为管理层使用按金额排序的专属榜单，复验时应以新口径为准。

### 2.4 建议对账样本

1. `S15` 高金额样本：用于核对管理层对高金额项目的感知是否与真实经营贡献一致。
2. `S09` 丢标样本：用于核对风险与归档结果的业务解释。
3. `S10` 放弃样本：用于核对归档口径是否与业务理解一致。

## 3. 经营负责人首轮记录

### 3.1 首屏读数

| 指标 | 当前值 | 对应来源 |
| --- | ---: | --- |
| 在手机会 | 358 | `funnel.totalOpenCount` |
| 敞口金额 | ¥35676.7万 | `funnel.totalOpenAmount` |
| 平均赢率 | 0% | `funnel.avgWinProbability` |
| 本周到期 | 0 | `riskSummary.dueThisWeek` |
| 最大盘子阶段 | 需求确认 | `funnel.stages` 最大 amount |
| 最大盘子阶段金额 | ¥35656.7万 | `qualified.amount` |

### 3.2 商机热区观察

当前经营负责人视图复用 `topRegions`，前 3 位仍为：杭州、山东、温台。

### 3.3 首轮判定

1. 当前机会盘规模、阶段集中度和补盘子方向可以回答。
2. 默认 tab 已切到 `customers`，与当前治理配置一致。
3. 但平均赢率与加权合同池都为 0，说明当前机会记录中的赢率填报完整性偏弱，不适合直接作为业务签字项；同日后续修正已在视图中显式提示未填赢率机会数，业务仍需确认是否先补数据或接受当前口径。

### 3.4 建议对账样本

1. `S15` 高金额样本：核对敞口金额与阶段感知。
2. `S07` 中标样本：核对漏斗“已转合同”口径。
3. `S08` 已签单样本：核对预测完成额与已完成额的业务解释。

## 4. 售前负责人首轮记录

### 4.1 首屏读数

| 指标 | 当前值 | 对应来源 |
| --- | ---: | --- |
| 总支撑工时 | 0h | `summary.totalSupportHours` |
| 活跃支撑项目 | 1 | `summary.activeSupportProjects` |
| 过载成员 | 0 | `summary.overloadedStaffCount` |
| 活跃支撑类型 | 2 | `summary.activeServiceTypes` |
| 方案复用覆盖率 | 0% | `summary.solutionReuseCoverageRate` |
| 方案使用项目数 | 0 | `summary.solutionUsageProjects` |

### 4.2 成员与项目观察

当前首轮快照中：

1. 成员负载 TOP：张伟、李芳，均为 `1` 个项目 / `1` 次服务 / `0h`
2. 关键项目：`智慧校园平台建设`，区域 `华北`，阶段 `opportunity`，`2` 次服务
3. 支撑类型分布：`analysis=1`、`design=1`

### 4.3 首轮判定

1. 售前负责人首屏结构与专属读模型已经贯通。
2. 当前数据能回答“有哪些项目正在支撑、有哪些支撑类型”，但无法有效回答“支撑工时是否过载”，因为当前快照里的工时字段均为 0。
3. 这更像售前服务记录的数据完整性问题，而不是当前读模型错误；同日后续修正已在视图中显式提示未填工时记录，业务陪跑时仍需重点核对源记录填报是否完整。

### 4.4 建议对账样本

1. `智慧校园平台建设`：核对项目级售前服务记录是否完整。
2. 张伟：核对成员负载与实际服务登记。
3. 李芳：核对成员负载与实际服务登记。

## 5. 首轮发现项

| 编号 | 分类 | 发现 | 当前判断 |
| --- | --- | --- | --- |
| DS-R1-01 | 规则待确认 | 管理层“区域贡献 TOP3”首轮记录时来自 `topRegions`，排序表现更像热点数量，不一定等同于金额贡献 TOP3 | 已转代码修正，待业务复验新口径 |
| DS-R1-02 | 数据完整性 | 售前负责人视图存在活跃支撑项目，但总支撑工时与成员工时均为 0h | 已转显性提示修正，仍需核对源服务记录是否未填工时 |
| DS-R1-03 | 数据完整性 | 经营负责人视图当前平均赢率为 0%，加权合同池也为 0 | 已转显性提示修正，仍需核对机会赢率字段是否普遍缺失 |

## 6. 当前结论

1. 当前 formal 基线已经足够支撑首轮业务陪跑，不需要再等待技术侧补门禁。
2. 下一步最有效动作不是改代码，而是按本记录中的样本建议组织一次真实业务账号陪跑。
3. 陪跑后应把通过项和差异项分别回写到指标对账记录与问题台账。
