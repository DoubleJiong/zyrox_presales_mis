# 2026-04-11 下一轮需求池清单

## 1. 目标

本文件用于从《需求修改清单验收收敛结论》中拆出“下一轮真正要开发”的需求池。

本文件只回答两个问题：

1. 哪些条目应直接进入下一轮需求排期。
2. 哪些条目当前还不应进入开发，而应先完成专项验证或直接关闭。

## 2. 纳入原则

下一轮开发池仅纳入以下两类事项：

1. 已明确属于新需求，且业务口径已经收敛的条目。
2. 已确认“部分落地”，下一轮需要继续闭环的条目。

以下事项不直接纳入下一轮开发池：

1. 已通过项。
2. 已驳回或明确不纳入项。
3. 仍处于“待核”状态、尚未形成专项样本结论的条目。

## 3. 下一轮直接开发池

| 优先级 | 来源编号 | 需求项 | 纳入原因 | 下一轮目标 |
| --- | --- | --- | --- | --- |
| P1 | 2.1 | 下钻功能 | 已明确属于需求项，不按现网 bug 管理，但业务侧已提出闭环要求 | 明确下钻入口、下钻层级、返回路径和各模块统一交互 |
| P1 | 2.2 | 项目类型按 OA 分类且可多选 | 已明确属于需求项，当前系统未按目标模型闭环 | 完成项目类型分类映射、录入编辑交互和数据口径统一 |
| P1 | 2.3 | 日期/金额排序 | 已部分落地，但项目页未完整闭环 | 补齐客户页、项目页的排序字段、排序规则和可见反馈 |
| P1 | 2.4 | 左侧/顶部固定 | 已部分落地，但固定栏能力未完整闭环 | 明确固定区域范围、滚动行为和不同页面的一致性体验 |
| P2 | 附加项 B | 周报 / 周报导出 | 已明确未落地，属于后续需求 | 明确周报生成口径、导出字段、下载入口和权限边界 |
| P2 | 附加项 C | 工作台标签收拢到工作台下方 | 已明确未落地，属于界面结构调整需求 | 完成工作台标签区重排，保证入口可见性与信息层级稳定 |
| P3 | 1.1 | 客户关键字去重提醒 | 已明确未落地，且属于低优先级需求 | 提供客户录入时的关键字去重提醒或相似客户提示能力 |

## 4. 下一轮不直接开发，但要先处理的前置项

### 4.1 先补专项验收，再决定是否开发

| 来源编号 | 需求项 | 当前状态 | 下一步 |
| --- | --- | --- | --- |
| 2.5-2.7 | 单击查看、按钮统一、重点项目标签 | 待核 | 先补现网专项样本，确认是“未做”还是“已做但证据不足” |
| 2.9-2.12 | 投标相关命名、字段去重、进度 100%、日期显示 | 待核 | 先补专项验收，再决定进入开发还是直接关闭 |
| 3.1 | 方案列表分页样式统一 | 待核 | 先确认现网样式差异和目标样式口径 |
| 附加项 A | 新建项目页面跳动 | 待核 | 先复核触发条件，再判断是需求优化还是现网缺陷 |
| 附加项 D | 合同管理新建页附件 / 签约模式 / 单位联动 | 待核 | 先按合同模块做专项复核，再决定是否纳入下一轮 |

### 4.2 不再进入开发池的关闭项

| 来源编号 | 需求项 | 当前结论 | 处理原则 |
| --- | --- | --- | --- |
| 2.8 | 新建项目“兼容状态”文案 | 已通过 | 本轮关闭，不再进入下一轮需求池 |
| 1.2-1.5 | 客户编号前缀统一、自定义字段、增加字段、客户页增加新建项目按钮 | 已驳回/不纳入 | 按驳回项关闭，不再进入下一轮需求池 |

## 5. 建议的下一轮排期结构

建议把下一轮需求池分为三个实施包，而不是继续按原始清单编号推进：

### 5.1 交互闭环包

1. `2.1` 下钻功能。
2. `2.4` 左侧/顶部固定。
3. 工作台标签收拢到工作台下方。

目标：先把高频操作路径和页面结构体验做顺。

### 5.2 主数据与列表能力包

1. `2.2` 项目类型按 OA 分类且可多选。
2. `2.3` 日期/金额排序。
3. `1.1` 客户关键字去重提醒。

目标：先补数据录入、列表使用和检索判断相关能力。

### 5.3 报表输出包

1. 周报。
2. 周报导出。

目标：单独作为报表能力包推进，避免与前台交互优化项串行耦合。

## 6. 统一结论

下一轮真正建议进入开发排期的，是 `2.1`、`2.2`、`2.3`、`2.4`、周报 / 周报导出、工作台标签收拢、`1.1` 共 7 类事项。

其中：

1. `2.1-2.4` 应视为主干需求，优先级高于其他未落地项。
2. 周报类能力和工作台标签收拢可作为第二梯队需求。
3. `1.1` 客户关键字去重提醒维持低优先级，不建议挤占主干闭环资源。
4. 所有“待核”项先补专项验收，再决定是否转入开发，不应直接进入下一轮需求池。

补充：

1. 三类未闭环事项的统一出口，已单独收口到《未闭环需求项闭环执行表》。
2. `2.1-2.4` 主干需求已单独拆分到《主干需求研发任务卡》。

## 7. 2026-04-11 开发回写

1. `2.1-2.4` 已在本轮完成开发实现并通过研发侧验证，不再作为“下一轮直接开发池”条目继续排期。
2. 下一轮直接开发池现阶段仅保留：附加项 B、附加项 C、`1.1`。
3. 待核项仍按专项验收清单处理，未通过后再决定是否回流开发池。
4. 第二梯队三项已补齐任务卡、建单精简版和专项验收清单，可按“规则验收 -> 建卡 -> 开发 -> 功能验收”顺序推进。
5. 第二梯队执行文档入口：
	- 《第二梯队需求研发任务卡》
	- 《第二梯队需求建单精简版》
	- 《第二梯队需求专项验收清单》