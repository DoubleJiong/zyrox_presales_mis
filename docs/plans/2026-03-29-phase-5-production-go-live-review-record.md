# 第五阶段正式投产评审记录

日期：2026-03-29

补充更新：2026-03-30

补充更新：2026-03-31

补充更新：2026-04-01

补充更新：2026-04-01（第三块收口）

## 1. 评审输入

1. 第四阶段候选版本验证报告
2. 第五阶段影子试运行场景与记录
3. 权限边界验证记录
4. 指标对账记录
5. 问题台账
6. 正式投产门禁清单

## 2. 四类门禁检查

### 业务门禁

结论：通过

依据：

1. 已完成 Excel 人工台账导入后的 16 个代表性样本核对
2. 已完成客户/项目/方案主链路的第二轮 UI 页面证据留档
3. 已完成 `/dashboard-screen` 页面卡片读数与接口、数据库复算一致性核对
4. 已在保留业务验收实例 `http://localhost:5004` 上执行 `corepack pnpm run verify:acceptance:5004`，22 条高频写路径稳定性扫雷全部通过
5. 2026-04-01 已在同一 `5004` 验收实例上补跑并通过 `tests/e2e/playwright/phase5-sample-visibility.spec.ts`，客户 / 项目 / 方案代表性样本可见性与刷新抽检 `3/3` 通过
6. 2026-04-01 已在同一 `5004` 验收实例上复跑整份 `tests/e2e/playwright/stability-sweep.spec.ts`，当前 `31/31` 通过，覆盖用户管理、项目管理、客户管理、方案管理、项目类型专用管理、项目跟进、方案关联、招投标、归档、方案评审等高频主链路
7. 2026-04-01 已继续将第三块“系统设置 + 人员管理”收口为可判定子模块清单：`/settings/users`、`/staff`、`数据权限配置`、`角色权限配置`、`系统日志` 均已拿到页面/API 级真实证据；原“数据备份”页面已明确降格为“基础数据维护/恢复出厂设置”，不得再按备份恢复系统宣称完成
8. 2026-04-01 在正式生产构建恢复后，`http://localhost:5004` 已重新切回 `next start -p 5004` 口径，`/api/health` 返回 `200`，扩展后的整份 `tests/e2e/playwright/stability-sweep.spec.ts` 已覆盖 `39` 条验收项
9. 2026-04-06 `verify:acceptance:5004` 已从“仅稳定性扫雷”升级为“稳定性扫雷 + 数据屏驾驶舱 formal 回归”的统一入口，并已在 `5004` 上实跑通过 `40 passed (1.7m)`；最新正式门禁口径应按 `39 + 1 = 40` 条页面级验收项理解，而不再只引用旧的 sweep 数量。

### 工程门禁

结论：通过

依据：`verify:release-candidate` 已通过；隔离影子环境中 `/api/health` 返回 `200`，且以显式 schema push 初始化后 `POST /api/db/seed` 已成功；保留验收实例 `5004` 上的 `verify:acceptance:5004` 当前已定义为统一页面级验收入口，覆盖 `tests/e2e/playwright/stability-sweep.spec.ts` 与 `tests/e2e/playwright/data-screen-formal.spec.ts` 两条正式回归线，且最新实跑结果为 `40 passed (1.7m)`。

补充说明：2026-04-01 已继续把 `5004` 上的页面级验收证据从“22 条高频写路径”提升到“39 条稳定性扫雷 + 3 条代表样本可见性”双重通过；同时 `idempotency_keys` 已纳入正式迁移并完成验收库建表，不再保留此前那类缺表日志风险。当前 `5004` 验收实例也已从临时 dev 口径重新恢复为正式 `next start -p 5004` 生产包口径。后续非阻断工程收尾中，根级鉴权入口已迁移到 `proxy`，正式 `build` 口径已切回 webpack，并重新生成 `.next/BUILD_ID`；基于该产物重启 `5004` 后，`/api/health` 返回 `200`，整份 `tests/e2e/playwright/stability-sweep.spec.ts` 已再次通过 `39/39`。

### 安全门禁

结论：通过

依据：自动化权限与身份审计验证通过；健康检查公共路径行为已在隔离真实运行时复验通过。

### 运维门禁

结论：通过

依据：发布、回滚、备份恢复基线已具备；本轮投产前业务对账与页面级验收已闭环，已具备进入正式发布窗口条件。

## 3. 风险接受项

本轮无可接受的阻断项豁免。当前剩余事项属于发布排期与观察执行，不属于阻断级缺陷豁免。

## 4. 评审结论

结论：通过

说明：

1. 当前版本已满足进入正式发布窗口的投产前门禁条件
2. 本记录表示“准予进入正式发布窗口”，不等同于“已执行正式投产发布动作”
3. 正式发布时间、值守安排和观察窗口仍需按发布计划单独执行
4. 2026-03-31 补充稳定性扫雷已覆盖用户管理、项目管理、项目策划团队管理、项目跟进记录、方案关联、招投标管理、归档信息编辑七类高频写路径的新增、编辑、删除、取消与重新关联场景，其中项目跟进记录已扩展到出差记录创建删除、附件上传、附件展示与链接打开，招投标管理已扩展到结果流转、团队成员、投标方案、投标工作日志四类核心写操作，归档页已扩展到中标/落标结果后的后置编辑链路
5. 2026-04-01 补充结论：客户 / 项目 / 方案三模块当前不仅具备“代表性整体验收通过”的依据，而且已经在最新 `5004` 正式生产口径验收包上通过扩展后的整份稳定性扫雷 `39/39`；因此三模块本体工作已进入收口阶段，剩余事项主要是正式发布窗口与发布后稳定观察执行
6. 2026-04-01 第三块“系统设置 + 人员管理”也已从边界模糊、证据不足的状态转为子模块级可发布清单；但评审措辞必须明确“基础数据维护”不等同于“备份恢复系统”，避免在正式会议纪要中形成能力误判

## 5. 后续动作

1. 按发布计划确定正式发布窗口
2. 明确发布值守与回滚责任人
3. 执行正式发布并开启稳定观察期