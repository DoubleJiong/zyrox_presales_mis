# 2026-04-11 需求修改清单验收收敛结论

## 1. 适用范围

本文件用于收敛“需求修改清单”的当前验收结果。

说明：

1. 本文件中的编号沿用需求修改清单原编号，不与 Sheet1 bug 台账编号混用。
2. 本文件的目标是完成需求口径收敛，不把“未落地需求”“需求驳回”“待专项验收”混写成“现网 bug”。
3. 本轮结论以“通过、部分落地、待核、驳回/不纳入、转后续需求池”五类收敛。

## 2. 收敛口径

| 收敛分类 | 定义 | 本轮处理原则 |
| --- | --- | --- |
| 通过 | 已形成明确通过证据 | 本轮关闭 |
| 部分落地 | 已有局部实现，但未形成完整闭环 | 保留为后续需求闭环项 |
| 待核 | 本轮未形成专项验收样本或通过证据 | 不下通过结论，进入后续专项验收 |
| 驳回/不纳入 | 已明确不符合售前系统设计原则或已被需求驳回 | 不纳入本轮不通过项 |
| 转后续需求池 | 属于新需求、低优先级需求或未落地需求，不按现网 bug 管理 | 转入后续需求排期 |

## 3. 收敛台账

| 编号 | 需求项 | 当前验收结果 | 收敛分类 | 本轮结论 |
| --- | --- | --- | --- | --- |
| 1.1 | 客户关键字去重提醒 | 未落地，低优先级需求 | 转后续需求池 | 不纳入本轮不通过项，转后续低优先级排期 |
| 1.2-1.5 | 客户编号前缀统一、自定义字段、增加字段、客户页增加新建项目按钮 | 已在清单中标明“需求驳回/不符合售前系统设计原则” | 驳回/不纳入 | 不纳入本轮不通过项，按驳回项关闭 |
| 2.1 | 下钻功能 | 未按新需求闭环，属于需求项 | 转后续需求池 | 不按现网 bug 处理，转需求闭环项 |
| 2.2 | 项目类型按 OA 分类且可多选 | 未按新需求闭环，属于需求项 | 转后续需求池 | 不按现网 bug 处理，转需求闭环项 |
| 2.3 | 日期/金额排序 | 部分落地；客户页已有最近互动时间、金额排序能力，但项目页未完整闭环 | 部分落地 | 保留为后续需求闭环项 |
| 2.4 | 左侧/顶部固定 | 部分落地；整体“固定栏”未做完整闭环确认 | 部分落地 | 保留为后续需求闭环项 |
| 2.5-2.7 | 单击查看、按钮统一、重点项目标签 | 本轮未形成通过证据 | 待核 | 不下通过结论，进入后续专项验收 |
| 2.8 | 新建项目“兼容状态”文案 | 通过；面向用户的项目 UI 已不再把 compat status 当一等字段暴露 | 通过 | 本轮关闭 |
| 2.9-2.12 | 投标相关命名、字段去重、进度 100%、日期显示 | 本轮未做专项现网样本验收 | 待核 | 统一保留为待核需求项 |
| 3.1 | 方案列表分页样式统一 | 未专项验收 | 待核 | 保留待核，不下通过结论 |
| 附加项 A | 新建项目页面跳动 | 本轮未复核 | 待核 | 保留待核，不下通过结论 |
| 附加项 B | 周报 / 周报导出 | 周报类功能未落地 | 转后续需求池 | 不按现网 bug 处理，转后续需求项 |
| 附加项 C | 工作台标签收拢到工作台下方 | 未落地 | 转后续需求池 | 不纳入本轮 bug 不通过项，转后续需求项 |
| 附加项 D | 合同管理新建页附件 / 签约模式 / 单位联动 | 本轮未复核 | 待核 | 保留待核，不下通过结论 |

## 4. 本轮收敛结果

### 4.1 已通过

1. `2.8` 新建项目“兼容状态”文案。

### 4.2 驳回或不纳入本轮不通过项

1. `1.2-1.5` 已明确属于“需求驳回/不符合售前系统设计原则”。
2. `2.1`、`2.2` 属于新需求闭环项，不按现网 bug 管理。

### 4.3 部分落地，需后续继续闭环

1. `2.3` 日期/金额排序。
2. `2.4` 左侧/顶部固定。

### 4.4 本轮未形成通过证据，统一保留待核

1. `3.1` 方案列表分页样式统一。
2. `2.5-2.7` 单击查看、按钮统一、重点项目标签。
3. `2.9-2.12` 投标相关命名、字段去重、进度 `100%`、日期显示。
4. 新建项目页面跳动。
5. 合同管理新建页附件、签约模式、单位联动。

### 4.5 转后续需求池，不按本轮 bug 不通过项处理

1. `1.1` 客户关键字去重提醒。
2. 周报 / 周报导出。
3. 工作台标签收拢到工作台下方。

## 5. 统一结论

本轮“需求修改清单”不再按单一“通过 / 不通过”口径管理，而收敛为以下结论：

1. 已有明确通过证据的，仅 `2.8` 一项。
2. `1.2-1.5` 已明确驳回，不纳入本轮不通过项。
3. `2.1`、`2.2` 以及若干未落地条目，本质上属于需求项或后续排期项，不按现网 bug 追责。
4. `2.3`、`2.4` 属于“部分落地但未完整闭环”，应以后续需求完善为目标，而不是作为已修复项关闭。
5. `3.1`、`2.5-2.7`、`2.9-2.12`、新建项目页面跳动、合同管理新建页联动能力，当前统一维持“待核”，待专项样本验收后再下结论。

## 6. 建议的后续动作

1. 把 `2.3`、`2.4` 单列到下一轮需求闭环清单，避免与 bug 修复项混排。
2. 为 `3.1`、`2.5-2.7`、`2.9-2.12`、合同管理新建页能力补一轮专项现网样本验收。
3. 将 `1.1`、周报类功能、工作台标签收拢统一转入后续需求排期，不继续占用本轮验收结论。

补充：

1. 下一轮开发池已单独拆分到《下一轮需求池清单》。
2. 三类未闭环事项已单独拆分到《未闭环需求项闭环执行表》。
3. 全部关联文档已统一收口到《需求闭环文档总索引》。

## 7. 2026-04-11 开发回写

1. `2.1-2.4` 已在本轮完成开发闭环，不再维持“转后续需求池”或“部分落地”状态。
2. 本轮实际落地内容包括：
	- `2.1`：项目列表、卡片、看板统一改为抽屉式下钻入口，并保留原页面详情入口。
	- `2.2`：项目类型补齐 OA 分类分组、多选协议、录入编辑闭环与详情/列表展示闭环。
	- `2.3`：项目页补齐开始日期、预计交付日期、预算金额、更新时间排序反馈。
	- `2.4`：项目筛选区改为顶部固定，项目列表补齐左侧首列与右侧操作列固定行为。
3. 本轮验证结果：`typecheck` 通过；项目类型编解码单测通过；`/api/projects` 与 `/api/projects/[id]` 聚焦接口测试通过。
4. 自本次回写起，`2.1-2.4` 统一视为“已开发完成，待业务验收”的状态，不再纳入下一轮主干开发池。