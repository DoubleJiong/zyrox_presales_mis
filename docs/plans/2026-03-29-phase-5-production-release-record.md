# 第五阶段正式投产发布记录

日期：2026-03-29

补充更新：2026-03-30

补充更新：2026-04-01

## 1. 发布决定

截至 2026-03-30，本轮仍未执行正式投产发布动作。

补充更新：2026-04-01 17:08 已基于最新 webpack 正式构建产物执行正式投产发布动作，当前版本已经在 `http://localhost:5004` 上按 `next start -p 5004` 生产口径对外提供服务。

## 2. 原因

1. 2026-03-30 时点虽已完成投产前业务验收与补充评审，但尚未进入实际发布窗口
2. 2026-04-01 17:08 前，发布值守、回滚观察和正式切换动作尚未执行

## 3. 已完成的发布前动作

1. `corepack pnpm run verify:release-candidate` 通过
2. 聚焦权限、审批、仪表盘、身份、健康检查的自动化证据套件通过
3. 隔离 PostgreSQL 环境已完成建表
4. 隔离环境 seed 已暴露并定位关键初始化问题
5. 已完成 Excel 人工台账导入后的业务样本对账
6. 已完成客户/项目/方案主链路第二轮 UI 页面证据留档
7. 已完成 `/dashboard-screen` 页面统计卡片与接口、数据库对账
8. 已在 `http://localhost:5004` 上补跑并通过 `tests/e2e/playwright/phase5-sample-visibility.spec.ts`，客户 / 项目 / 方案代表性样本可见性 `3/3` 通过
9. 已在 `http://localhost:5004` 上复跑并通过整份 `tests/e2e/playwright/stability-sweep.spec.ts`，当前扩展为 `39/39` 通过
10. 已补齐 `idempotency_keys` 正式迁移与验收库建表，消除项目 / 客户创建链路的环境缺表噪音
11. `5004` 验收实例已重新恢复为正式 `next start -p 5004` 生产包口径，`/api/health` 返回 `200`
12. 第三块“系统设置 + 人员管理”已完成发布级收口：`/settings/users`、`/staff`、`数据权限配置`、`角色权限配置`、`系统日志` 均已验证可发布；原“数据备份”页面当前只可按“基础数据维护/恢复出厂设置”纳入发布口径
13. 非阻断工程收尾中，Next.js 根级鉴权入口已从 `middleware` 迁移到 `proxy`，正式 `build` 已切回 `next build --webpack`，并重新生成 `.next/BUILD_ID`，以绕开 Turbopack 下 `/_global-error` 的独立构建故障
14. 基于新的 webpack 构建产物重启 `5004` 正式实例后，`/api/health` 再次返回 `200`，整份 `tests/e2e/playwright/stability-sweep.spec.ts` 再次通过 `39/39`
15. 2026-04-01 17:08 已停止旧 `5004` 进程并拉起新正式产物；当前监听进程开始时间为 `2026-04-01 17:08:57`
16. 新正式产物启动后，`/api/health` 返回 `200`，`/api/health/ready` 返回 `200`
17. 已在新正式产物上执行最小冒烟 `tests/e2e/playwright/smoke.spec.ts`，结果为 `3/3 passed`

## 4. 发布窗口结论

当前候选版本已于 2026-04-01 17:08 进入正式发布窗口并完成正式投产发布动作。

补充说明：截至当前，客户 / 项目 / 方案三模块以及第三块“系统设置 + 人员管理”均已进入收口阶段，当前未完成事项仅剩“本次新正式产物发布后的稳定观察期关闭”一项。正式评审纪要中应明确：当前“基础数据维护”不等同于“备份恢复系统”。