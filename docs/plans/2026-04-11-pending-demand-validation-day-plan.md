# 2026-04-11 待核需求项专项验收当日执行安排

日期：2026-04-11

适用范围：

1. `2.5-2.7` 项目管理交互一致性。
2. `2.9-2.12` 投标字段与展示口径。
3. `3.1` 方案列表分页样式统一。
4. 附加项 A：新建项目页面跳动。
5. 附加项 D：合同管理新建页附件 / 签约模式 / 单位联动。

复用依据：[docs/plans/2026-04-11-pending-demand-validation-checklist.md](c:/Users/doubl/Desktop/%E5%B8%82%E5%9C%BA%E9%83%A8/2026/%E9%83%A8%E9%97%A8%E4%BF%A1%E6%81%AF%E5%8C%96/presales_app/workspace/app_code/docs/plans/2026-04-11-pending-demand-validation-checklist.md)

## 1. 今日目标

今天只做一件事：把全部“待核”项跑出明确结论。

今日结束前，每一项只能落到以下两类结果之一：

1. 通过，直接关闭。
2. 不通过，转开发闭环。

不保留“还没看完”“后面再补”“先记一下”这类开放状态。

## 2. 角色分工

| 责任项 | 当前责任岗 | 今日产出 |
| --- | --- | --- |
| 验收组织人 | 产品 / 项目负责人 | 拉起分工、汇总结论、确认口径归档 |
| 项目管理验收人 | 测试 + 业务验收人 | 输出 `2.5-2.7`、`2.9-2.12` 验收结论 |
| 方案模块验收人 | 测试 + 前端 | 输出 `3.1` 验收结论 |
| 新建项目页复核人 | 前端 + 测试 | 输出“页面跳动”是否可复现及归因 |
| 合同模块验收人 | 测试 + 合同模块责任开发 | 输出附件 / 签约模式 / 单位联动结论 |
| 台账更新人 | 产品 / PMO | 同步改写收敛文档与需求池文档 |

## 3. 今日执行顺序

### 3.1 第一轮：主干模块先跑

建议先完成以下三组：

1. `2.5-2.7` 项目管理交互一致性。
2. `2.9-2.12` 投标字段与展示口径。
3. `3.1` 方案列表分页样式统一。

要求：

1. 每组跑完立即出结论，不等全部结束后再统一回填。
2. 若发现明确不通过，现场直接登记“转开发”，不继续拖延。

### 3.2 第二轮：附加项复核

随后完成以下两组：

1. 新建项目页面跳动。
2. 合同管理新建页联动能力。

要求：

1. 页面跳动要先确认是否稳定复现。
2. 合同模块三项能力要分别给出结论，不能合并成一句“基本可以”。

## 4. 分项执行安排

### 4.1 项目管理组

负责范围：

1. `2.5-2.7`
2. `2.9-2.12`

建议参与角色：

1. 1 名测试。
2. 1 名熟悉项目管理业务的业务验收人。
3. 1 名前后端责任开发待命答疑。

今日输出：

1. 每个编号对应的结论。
2. 截图证据。
3. 若不通过，补“现状表现 + 目标口径 + 转开发建议”。

### 4.2 方案模块组

负责范围：

1. `3.1` 方案列表分页样式统一。

建议参与角色：

1. 1 名测试。
2. 1 名前端责任人。

今日输出：

1. 与客户 / 项目主列表页的对照截图。
2. 分页样式是否统一的明确结论。

### 4.3 新建项目页复核组

负责范围：

1. 附加项 A：页面跳动。

建议参与角色：

1. 1 名前端。
2. 1 名测试。

今日输出：

1. 是否能稳定复现。
2. 触发条件。
3. 若复现，初步归因属于布局问题、联动问题还是浏览器兼容问题。

### 4.4 合同模块组

负责范围：

1. 附加项 D：附件上传。
2. 签约模式。
3. 单位联动。

建议参与角色：

1. 1 名测试。
2. 1 名合同模块责任开发。
3. 必要时 1 名业务确认人。

今日输出：

1. 三项能力逐项结论。
2. 至少 1 组附件上传回显证据。
3. 至少 2 组客户 / 单位联动样本结论。

## 5. 今日时间切片建议

| 时间段 | 动作 | 产出要求 |
| --- | --- | --- |
| 上午第一时段 | 分工确认、账号与样本确认 | 确认每组责任人和样本 |
| 上午第二时段 | 跑 `2.5-2.7`、`2.9-2.12` | 产出项目管理两组结论 |
| 下午第一时段 | 跑 `3.1`、页面跳动 | 产出方案分页与新建项目页复核结论 |
| 下午第二时段 | 跑合同模块三项能力 | 产出合同模块结论 |
| 收工前 | 台账统一改写 | 把“待核”全部改成“通过”或“转开发” |

## 6. 收工前检查表

今天结束前必须确认：

1. 所有待核项都已有最终结论。
2. 所有不通过项都已写明是否转开发。
3. 所有通过项都已回写到收敛文档。
4. 不再残留“待核”状态。

## 7. 对外同步口径

若今天按本安排执行完毕，对外可以使用以下口径：

“4.9 问题清单中原先处于待核状态的需求项，已在今日完成专项验收安排；执行完成后，将全部收口为‘通过关闭’或‘转开发闭环’，不再保留待核状态。”

## 8. 当前结论

待核项现在已经具备当日可执行的分工版安排。下一步不再是补文档，而是按本执行表拉人、跑样本、回写结论。