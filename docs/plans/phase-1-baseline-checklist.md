# 第一阶段基线台账

日期：2026-03-29

适用范围：售前管理系统第一阶段止血与安全兜底

说明：本台账用于跟踪第一阶段实际纳入执行的风险项、目标状态、验证方式和完成情况。未经确认，不在本台账外扩展第一阶段范围。

| 编号 | 风险项 | 对应问题编号 | 当前状态 | 目标状态 | 负责模块 | 验证方法 | 完成日期 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| B01 | 数据库连接串硬编码 | 01 | 已完成（源码已移除硬编码，改为强制读取 `DATABASE_URL`） | 仅允许通过环境变量读取，不保留源码硬编码凭据 | 配置 / 数据库 | 代码检查 + `typecheck` + 配置单元测试 | 2026-03-29 |
| B02 | 公开测试后门接口 | 02 | 已完成（已取消公开放行，改为默认拒绝，仅允许显式本地开发开关 + 测试密钥） | `/api/test/setup-user` 删除或严格受控，不再公开放行 | 认证 / 中间件 / 测试接口 | API 负路径验证 | 2026-03-29 |
| B03 | 双 JWT 实现 | 03 | 已完成（`jwt.ts` 已成为唯一权威认证实现，`auth.ts` 仅保留兼容门面） | 统一为唯一认证实现和唯一 secret 来源 | 认证 / 会话 | JWT 单元测试 + 会话 API 测试 | 2026-03-29 |
| B04 | `TEST_MODE` 鉴权绕过 | 04 | 已完成（旧 `auth.ts` 的 `TEST_MODE` 旁路已移除） | 生产与常规开发路径中不允许通过 `TEST_MODE` 绕过业务鉴权 | 认证 / 鉴权 | 代码检查 + 会话 API 测试 | 2026-03-29 |
| B05 | query token 认证入口 | 15 | 已完成（默认 token 提取已拒绝 query token，SSE 也不再接受 query token） | 默认拒绝 URL query token，认证仅允许受控来源 | 认证 / SSE | JWT 单元测试 + SSE/API 测试 | 2026-03-29 |
| B06 | 黑名单校验 fail-open | 14 | 已完成（黑名单校验失败不再默认放行，会返回显式不可用错误） | 注销与黑名单校验失败时不得默认为安全通过 | 认证 / 会话 | 会话 API 测试 + 中间件验证 | 2026-03-29 |
| B07 | 默认口令与登录页测试账号暴露 | 11 | 已完成（登录页不再展示测试账号；人员创建已取消 `123456` 默认口令回退，改为显式输入初始密码并走服务端校验） | 登录页不展示测试账号和默认口令，默认口令策略被移除或受控 | 登录 / 人员管理 | 页面冒烟 + 接口检查 + API 负路径测试 | 2026-03-29 |
| B08 | 无负责人项目可见性底线缺失 | 06 | 已完成（普通成员默认不再获得无负责人项目可见性；仪表盘与合同材料同时补了最小权限兜底） | 普通成员默认不可查看无负责人项目 | 项目 / 权限 | 策略测试 + API 测试 + `typecheck` | 2026-03-29 |
| B09 | 生产构建失败 | 21 | 已完成（合同上传构建期副作用已隔离，`pnpm build` 可通过） | `pnpm build` 可以稳定通过 | 构建 / 合同上传 | 构建安全测试 + `build` 验证 | 2026-03-29 |
| B10 | 开发态登录页不稳定 | 22, 23 | 已完成（开发模式已强制回退 `--webpack`，登录页 smoke 和真实浏览器脚本验证通过） | 登录页在本地开发与浏览器自动化下可稳定渲染 | 前端 / E2E | Playwright 冒烟 + 手工验证 | 2026-03-29 |
| B11 | 最小测试基线缺失 | 20 | 已完成（本地 `typecheck`/`test:unit`/`test:api`/`test:smoke`/`verify:phase1` 可运行，最小 CI 工作流已建立） | 建立最小单元、API、冒烟测试和 CI 门槛 | 测试 / CI | 本地验证命令 + CI 工作流检查 | 2026-03-29 |

## 备注

1. 第一阶段不处理完整业务模型重构。
2. 第一阶段不处理 `scheme` 与 `solution` 的最终收敛。
3. 第一阶段只做权限底线兜底，不做完整权限系统重构。
4. 若执行中发现任务进入第二阶段范围，必须单独评审后再决定是否纳入。